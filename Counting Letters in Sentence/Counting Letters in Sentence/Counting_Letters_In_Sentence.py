import sys

class text_Experimentation:
    """
    Text experimentation class
    """
    def __init__(self):

        def start(self):
            """
            This is an experiment to see if I can create a letter counter in an input string without using the '.count. list function which is built into Python.
        
            """

            sentence = input().lower()
            length = len(sentence)
            letter_Counter = list("abcdefghijklmnopqrstuvwxyz")
            already_Counted = 0

            for i in sentence:
                if i == ' ':
                    continue
                x = (letter_Counter.index(i)+1) #Variable to check if the letter already has a occured
                counter = 1
                if i == 'a':

                    already_Counted = isinstance(letter_Counter[x],str)

                    if already_Counted == True:
                        letter_Counter.insert(x,counter)
                    else:

                        counter = (letter_Counter[x]+1)
                        letter_Counter.pop(x)
                        letter_Counter.insert(x,counter)
                elif i == 'b':

                    already_Counted = isinstance(letter_Counter[x],str)

                    if already_Counted == True:
                        letter_Counter.insert(x,counter)
                    else:

                        counter = (letter_Counter[x]+1)
                        letter_Counter.pop(x)
                        letter_Counter.insert(x,counter)
                elif i == 'c':

                    already_Counted = isinstance(letter_Counter[x],str)

                    if already_Counted == True:
                        letter_Counter.insert(x,counter)
                    else:

                        counter = (letter_Counter[x]+1)
                        letter_Counter.pop(x)
                        letter_Counter.insert(x,counter)
                elif i == 'd':

                    already_Counted = isinstance(letter_Counter[x],str)

                    if already_Counted == True:
                        letter_Counter.insert(x,counter)
                    else:

                        counter = (letter_Counter[x]+1)
                        letter_Counter.pop(x)
                        letter_Counter.insert(x,counter)
                elif i == 'e':

                    already_Counted = isinstance(letter_Counter[x],str)

                    if already_Counted == True:
                        letter_Counter.insert(x,counter)
                    else:

                        counter = (letter_Counter[x]+1)
                        letter_Counter.pop(x)
                        letter_Counter.insert(x,counter)
                elif i == 'f':

                    already_Counted = isinstance(letter_Counter[x],str)

                    if already_Counted == True:
                        letter_Counter.insert(x,counter)
                    else:

                        counter = (letter_Counter[x]+1)
                        letter_Counter.pop(x)
                        letter_Counter.insert(x,counter)
                elif i == 'g':

                    already_Counted = isinstance(letter_Counter[x],str)

                    if already_Counted == True:
                        letter_Counter.insert(x,counter)
                    else:

                        counter = (letter_Counter[x]+1)
                        letter_Counter.pop(x)
                        letter_Counter.insert(x,counter)
                elif i == 'h':

                    already_Counted = isinstance(letter_Counter[x],str)

                    if already_Counted == True:
                        letter_Counter.insert(x,counter)
                    else:

                        counter = (letter_Counter[x]+1)
                        letter_Counter.pop(x)
                        letter_Counter.insert(x,counter)
                elif i == 'i':

                    already_Counted = isinstance(letter_Counter[x],str)

                    if already_Counted == True:
                        letter_Counter.insert(x,counter)
                    else:

                        counter = (letter_Counter[x]+1)
                        letter_Counter.pop(x)
                        letter_Counter.insert(x,counter)
                elif i == 'j':

                    already_Counted = isinstance(letter_Counter[x],str)

                    if already_Counted == True:
                        letter_Counter.insert(x,counter)
                    else:

                        counter = (letter_Counter[x]+1)
                        letter_Counter.pop(x)
                        letter_Counter.insert(x,counter)
                elif i == 'k':

                    already_Counted = isinstance(letter_Counter[x],str)

                    if already_Counted == True:
                        letter_Counter.insert(x,counter)
                    else:

                        counter = (letter_Counter[x]+1)
                        letter_Counter.pop(x)
                        letter_Counter.insert(x,counter)
                elif i == 'l':

                    already_Counted = isinstance(letter_Counter[x],str)

                    if already_Counted == True:
                        letter_Counter.insert(x,counter)
                    else:

                        counter = (letter_Counter[x]+1)
                        letter_Counter.pop(x)
                        letter_Counter.insert(x,counter)
                elif i == 'm':

                    already_Counted = isinstance(letter_Counter[x],str)

                    if already_Counted == True:
                        letter_Counter.insert(x,counter)
                    else:

                        counter = (letter_Counter[x]+1)
                        letter_Counter.pop(x)
                        letter_Counter.insert(x,counter)
                elif i == 'n':

                    already_Counted = isinstance(letter_Counter[x],str)

                    if already_Counted == True:
                        letter_Counter.insert(x,counter)
                    else:

                        counter = (letter_Counter[x]+1)
                        letter_Counter.pop(x)
                        letter_Counter.insert(x,counter)
                elif i == 'o':

                    already_Counted = isinstance(letter_Counter[x],str)

                    if already_Counted == True:
                        letter_Counter.insert(x,counter)
                    else:

                        counter = (letter_Counter[x]+1)
                        letter_Counter.pop(x)
                        letter_Counter.insert(x,counter)
                elif i == 'p':

                    already_Counted = isinstance(letter_Counter[x],str)

                    if already_Counted == True:
                        letter_Counter.insert(x,counter)
                    else:

                        counter = (letter_Counter[x]+1)
                        letter_Counter.pop(x)
                        letter_Counter.insert(x,counter)
                elif i == 'q':

                    already_Counted = isinstance(letter_Counter[x],str)

                    if already_Counted == True:
                        letter_Counter.insert(x,counter)
                    else:

                        counter = (letter_Counter[x]+1)
                        letter_Counter.pop(x)
                        letter_Counter.insert(x,counter)
                elif i == 'r':

                    already_Counted = isinstance(letter_Counter[x],str)

                    if already_Counted == True:
                        letter_Counter.insert(x,counter)
                    else:

                        counter = (letter_Counter[x]+1)
                        letter_Counter.pop(x)
                        letter_Counter.insert(x,counter)
                elif i == 's':

                    already_Counted = isinstance(letter_Counter[x],str)

                    if already_Counted == True:
                        letter_Counter.insert(x,counter)
                    else:

                        counter = (letter_Counter[x]+1)
                        letter_Counter.pop(x)
                        letter_Counter.insert(x,counter)
                elif i == 't':

                    already_Counted = isinstance(letter_Counter[x],str)

                    if already_Counted == True:
                        letter_Counter.insert(x,counter)
                    else:

                        counter = (letter_Counter[x]+1)
                        letter_Counter.pop(x)
                        letter_Counter.insert(x,counter)
                elif i == 'u':

                    already_Counted = isinstance(letter_Counter[x],str)

                    if already_Counted == True:
                        letter_Counter.insert(x,counter)
                    else:

                        counter = (letter_Counter[x]+1)
                        letter_Counter.pop(x)
                        letter_Counter.insert(x,counter)
                elif i == 'v':

                    already_Counted = isinstance(letter_Counter[x],str)

                    if already_Counted == True:
                        letter_Counter.insert(x,counter)
                    else:

                        counter = (letter_Counter[x]+1)
                        letter_Counter.pop(x)
                        letter_Counter.insert(x,counter)
                elif i == 'w':

                    already_Counted = isinstance(letter_Counter[x],str)

                    if already_Counted == True:
                        letter_Counter.insert(x,counter)
                    else:

                        counter = (letter_Counter[x]+1)
                        letter_Counter.pop(x)
                        letter_Counter.insert(x,counter)
                elif i == 'x':

                    already_Counted = isinstance(letter_Counter[x],str)

                    if already_Counted == True:
                        letter_Counter.insert(x,counter)
                    else:

                        counter = (letter_Counter[x]+1)
                        letter_Counter.pop(x)
                        letter_Counter.insert(x,counter)
                elif i == 'y':

                    already_Counted = isinstance(letter_Counter[x],str)

                    if already_Counted == True:
                        letter_Counter.insert(x,counter)
                    else:

                        counter = (letter_Counter[x]+1)
                        letter_Counter.pop(x)
                        letter_Counter.insert(x,counter)
                elif i == 'z':

                    already_Counted = isinstance(letter_Counter[x],str)

                    if already_Counted == True:
                        letter_Counter.insert(x,counter)
                    else:

                        counter = (letter_Counter[x]+1)
                        letter_Counter.pop(x)
                        letter_Counter.insert(x,counter)
                else:
                    continue

            print("Your sentence is " + str(length) + " characters long.")
            print("The letter breakdown is")
            print(letter_Counter)

            #Temporary cheat by using .count to include a word counter
            wordCounter = sentence.count(' ')
            print("\nThe number of words in that sentence is: " + str(wordCounter+1) + ". ")



        #x = ", ".join(["spam", "eggs", "ham"])
        #print(x)
        #prints "spam, eggs, ham"
        #Could use something like this to print numbers of letters found and only if the letter is found.

text_Experimentation.start()
